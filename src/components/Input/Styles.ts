import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({

    entrada: {
        width: 320,
        height: 50,
        borderBottomWidth: 2,
        borderBottomColor: '#5b5b5b',
        paddingBottom: 5,
        color: "#ffffff",
        marginBottom: 20,
        fontSize: 16,
    }
});